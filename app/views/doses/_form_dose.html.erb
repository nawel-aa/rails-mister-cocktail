<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.association :ingredient %>
  <%= f.input :description, label: "Amount" %>
  <%= f.button :submit, value: 'Add ingredient', class: 'btn btn-primary' %>
<% end %>
